apply plugin: 'com.android.library'

android {
    compileSdkVersion 28

    defaultConfig {
        //noinspection MinSdkTooLow
        minSdkVersion 1
        versionName "1.0.0"
        versionCode 100
    }
}